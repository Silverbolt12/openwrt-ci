name: Build Lucky APK for IPQ6000

on:
  workflow_dispatch: # 手动触发

env:
  BASE_URL: https://downloads.openwrt.org/snapshots/targets/qualcommax/ipq60xx/
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Initialization Environment
        run: |
          sudo apt update
          sudo apt install -y build-essential libncurses5-dev gawk git gettext libssl-dev xsltproc wget unzip qemu-utils curl
          sudo timedatectl set-timezone "$TZ"

      - name: Setup OpenWrt SDK & Dynamic Link
        run: |
          # 1. 尝试多种正则模式匹配 SDK 文件名
          INDEX_HTML=$(curl -sL ${{ env.BASE_URL }})
          SDK_NAME=$(echo "$INDEX_HTML" | grep -oE 'openwrt-sdk-qualcommax-ipq60xx_gcc-[^"> ]+\.tar\.xz' | head -n 1)
          
          if [ -z "$SDK_NAME" ]; then
            echo "错误：无法在索引页匹配到 SDK 文件名！"
            echo "尝试备用抓取方式..."
            SDK_NAME=$(echo "$INDEX_HTML" | grep -o 'openwrt-sdk-qualcommax-ipq60xx[^"]*.tar.xz' | head -n 1)
          fi

          if [ -z "$SDK_NAME" ]; then
            echo "彻底失败，输出网页源码前20行调试："
            echo "$INDEX_HTML" | head -n 20
            exit 1
          fi

          SDK_URL="${{ env.BASE_URL }}${SDK_NAME}"
          echo "成功定位 SDK: $SDK_URL"
          
          # 2. 下载并解压
          wget -q "$SDK_URL" -O sdk.tar.xz
          mkdir sdk && tar -xJf sdk.tar.xz -C sdk --strip-components=1

      - name: Install Feeds & Golang Support
        run: |
          cd sdk
          # 添加 Lucky 源码
          echo "src-git lucky https://github.com/gdy666/luci-app-lucky.git" >> feeds.conf.default
          
          ./scripts/feeds update -a
          ./scripts/feeds install -a
          
          # 核心修正：Lucky 是 Go 写的，必须显式安装 golang 工具链
          ./scripts/feeds install golang

      - name: Configure & Compile Lucky APK
        run: |
          cd sdk
          # 1. 开启 APK 模式
          echo "CONFIG_USE_APK=y" > .config
          
          # 2. 选中 Lucky 及其插件
          echo "CONFIG_PACKAGE_lucky=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-lucky=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-lucky-zh-cn=y" >> .config
          
          # 3. 刷新配置并编译
          make defconfig
          # 使用 V=s 查看详细日志，-j1 提高成功率（SDK 环境下 -j1 依然很快）
          make package/lucky/compile V=s -j$(nproc) || make package/lucky/compile V=s -j1

      - name: Organize & Upload Artifacts
        if: success()
        run: |
          mkdir -p publish_apk
          # 搜寻所有生成的 lucky 相关 apk 文件
          find sdk/bin/ -name "*lucky*.apk" -exec cp {} publish_apk/ \;
          
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Lucky-APK-IPQ6000
          path: publish_apk/*
